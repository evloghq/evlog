---
title: 'Libraries'
description: "Ship evlog inside an npm package so every host, and every agent calling it, gets errors with a code, a cause, and a fix instead of a string."
navigation:
  title: 'Overview'
  icon: i-lucide-package
links:
  - label: Emitting Events
    icon: i-lucide-radio
    to: /libraries/emitting
    color: neutral
    variant: subtle
  - label: Structured Errors
    icon: i-lucide-shield-alert
    to: /libraries/errors
    color: neutral
    variant: subtle
  - label: Errors Agents Can Act On
    icon: i-lucide-bot
    to: /libraries/agents
    color: neutral
    variant: subtle
  - label: Exporting the Catalog
    icon: i-lucide-file-output
    to: /libraries/exporting
    color: neutral
    variant: subtle
  - label: Migrating
    icon: i-lucide-route
    to: /libraries/migrating
    color: neutral
    variant: subtle
  - label: Testing
    icon: i-lucide-flask-conical
    to: /libraries/testing
    color: neutral
    variant: subtle
---

Your package throws `new Error('Not Found')` when GitHub answers 404. The host catches a string. Its dashboard cannot count it, its support search cannot find it, and the agent that called your tool reads "not found" as "the repository does not exist" when the real cause is a token that cannot see a private repo. The agent retries with a different owner, fails again, opens your docs, and spends a few thousand tokens learning what one sentence in the error could have said.

A catalog puts that sentence in the error. This is what the two versions hand a consumer:

::code-group
```typescript [Before: a bare Error]
throw new Error('Not Found')

// what the host or the agent receives
// Error: Not Found
```
```typescript [After: a catalog error]
throw githubToolsErrors.NOT_FOUND({ detail: 'Not Found' })

// what the host or the agent receives, via parseError() or the tool result
// {
//   code: 'github_tools.NOT_FOUND',
//   status: 404,
//   message: 'GitHub resource not found (404): Not Found',
//   why: 'Either the resource does not exist, or the token cannot see it.
//         GitHub deliberately returns 404 instead of 403 for private
//         resources the token has no access to.',
//   fix: 'Check the owner/repo/number input first. If it is correct,
//         the token lacks access: grant the repository to the PAT or
//         App installation, or use a Connect subject that has access.',
//   link: 'https://docs.github.com/.../troubleshooting-the-rest-api#404-not-found-for-an-existing-resource'
// }
```
::

The second error is the same 404. It costs the consumer one read instead of a loop, and it costs you the four sentences you would otherwise write in a support reply.

::callout{icon="i-lucide-github" color="neutral"}
The `github_tools` examples in this section come from [`@github-tools/sdk`](https://github.com/vercel-labs/github-tools), a typed GitHub tool layer for AI agents that ships an evlog catalog of eleven errors. It is a real package with real consumers, so its entries show what a catalog looks like after contact with production. Where a page needs a generic scaffold instead, it uses `mylib`.
::

::prompt
---
description: Add evlog to my library or SDK
icon: i-lucide-package
actions:
  - copy
  - cursor
  - claude
---

Set up evlog in an npm library that runs inside host applications.

- Ship errors as a `defineErrorCatalog()` with a prefix the package owns (e.g. `mylib.RATE_LIMITED`). Every entry carries `why` (the technical cause, including the non-obvious one), `fix` (an imperative a person or an agent can execute), and `link` (a stable docs anchor)
- Wrap upstream failures into catalog errors with `cause` set to the original and request coordinates in `internal`
- Treat catalog codes as wire format: additive changes only, deprecate before removing
- Declare `evlog` as a peer dependency pinned to one major, never call `initLogger()`: the host owns drains, sampling, and redaction
- Emit with the global `log` API or `createLogger()`, always with the package name in a field the host can filter on
- Inside a host request handler, accept the request logger as an `AuditableLogger` parameter instead of creating one
- Generate an error reference (Markdown and JSON) from the catalog and publish it with the docs
- In tests, collect events with a drain function or `evlog/memory`, and assert error codes through `factory.code`

Start: https://www.evlog.dev/libraries/overview
::

## What this section answers

| You are about to… | Read |
| --- | --- |
| Log from library code without stealing the host's configuration | [Emitting Events](/libraries/emitting) |
| Replace `throw new Error()` with a catalog the host can route | [Structured Errors](/libraries/errors) |
| Write `why` and `fix` so an agent recovers without opening your docs | [Errors Agents Can Act On](/libraries/agents) |
| Turn the catalog into a reference page, a JSON file, and an `llms.txt` section | [Exporting the Catalog](/libraries/exporting) |
| Convert an existing package with thirty bare throws, in order | [Migrating](/libraries/migrating) |
| Stop a renamed code or field from breaking a host silently | [Testing](/libraries/testing) |

## The contract

Your package emits. The host configures. Where events go, which ones survive sampling, and what gets redacted is decided by the application's `initLogger()` call, and library code never makes that call: `initLogger()` writes process-wide state shared by every evlog copy of the same major, and the last call wins. A library that calls it at import time replaces the host's drain for the whole process with a single `require`.

That one rule settles the decisions on [Emitting Events](/libraries/emitting): which API you use, where the package name goes, and why a library accepts a request logger as a parameter instead of building one. Nothing on the error pages touches configuration: a catalog is a module of factories, and throwing one is what makes the host's pipeline useful.

## What it costs

`evlog` becomes a peer dependency of your package, so a host that never installed it gets one more install. The process shares one configuration per evlog major, so you pin your peer range to a single major and document it. Each catalog entry is four sentences you maintain: the `why` and `fix` go stale when the upstream API changes, and a wrong `fix` misleads an agent faster than no `fix` at all. And the emit-only contract is a real constraint: your package cannot route its own events anywhere, which is the point.

For the API underneath this section, [Structured Errors](/learn/structured-errors) covers `createError` and catalogs as an application uses them, and [Wide Events](/learn/wide-events) covers the event model.
