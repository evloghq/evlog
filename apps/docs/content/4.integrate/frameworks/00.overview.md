---
title: Framework Integrations
description: Choose an evlog integration for Nuxt, Next.js, SvelteKit, Hono, Express, or another listed framework. Follow its setup and request-logger access guide.
navigation:
  title: Overview
  icon: i-lucide-layout-grid
---

Choose your framework from the table below. `log.set()`, `createError()` and `parseError()` use the same core APIs, while initialization and request-logger access depend on the integration. Some stacks have a module or middleware, others need the manual setup shown in their guide.

::callout{icon="i-lucide-globe" color="neutral"}
No HTTP framework? Use [Standalone TypeScript](/integrate/frameworks/standalone) for scripts, libraries, and workers, and [Cloudflare Workers](/integrate/frameworks/cloudflare-workers) on the edge.
::

## Pick your framework

| Framework | Import | Type | Logger Access | Status |
|-----------|--------|------|---------------|--------|
| [Nuxt](/integrate/frameworks/nuxt) | `evlog/nuxt` | Module | `useLogger(event)` | Stable |
| [Next.js](/integrate/frameworks/nextjs) | `evlog/next` | Factory | `useLogger()` | Stable |
| [SvelteKit](/integrate/frameworks/sveltekit) | `evlog/sveltekit` | Hooks | `event.locals.log` / `useLogger()` | Stable |
| [Nitro](/integrate/frameworks/nitro) | `evlog/nitro` | Module | `useLogger(event)` | Stable |
| [TanStack Start](/integrate/frameworks/tanstack-start) | `evlog/nitro/v3` | Module | `useRequest().context.log` | Stable |
| [TanStack Router](/integrate/frameworks/tanstack-start) | `evlog/nitro/v3` | Module | Via TanStack Start (uses Nitro v3) | Stable |
| [React Router](/integrate/frameworks/react-router) | `evlog/react-router` | Middleware | `context.get(loggerContext)` / `useLogger()` | Stable |
| [NestJS](/integrate/frameworks/nestjs) | `evlog/nestjs` | Module | `useLogger()` | Stable |
| [Express](/integrate/frameworks/express) | `evlog/express` | Middleware | `req.log` / `useLogger()` | Stable |
| [Hono](/integrate/frameworks/hono) | `evlog/hono` | Middleware | `c.get('log')` / `useLogger()` | Stable |
| [Fastify](/integrate/frameworks/fastify) | `evlog/fastify` | Plugin | `request.log` / `useLogger()` | Stable |
| [Elysia](/integrate/frameworks/elysia) | `evlog/elysia` | Plugin | `log` (context) / `useLogger()` | Stable |
| [oRPC](/integrate/frameworks/orpc) | `evlog/orpc` | Handler wrapper + middleware | `context.log` / `useLogger()` | Stable |
| [Cloudflare Workers](/integrate/frameworks/cloudflare-workers) | `evlog/workers` | Factory | `createWorkersLogger()` | Stable |
| [AWS Lambda](/integrate/frameworks/aws-lambda) | `evlog` | Manual | `createLogger()` / `createRequestLogger()` | Guide |
| [Standalone](/integrate/frameworks/standalone) | `evlog` | Manual | `createLogger()` / `createRequestLogger()` | Stable |
| [Astro](/integrate/frameworks/astro) | `evlog` | Manual | `createRequestLogger()` | Guide |
| [Custom](/extend/custom-framework) | `evlog/toolkit` | Build your own | `createMiddlewareLogger()` | Beta |

## What differs from one framework to the next

Two things differ per framework: how you start evlog, and how a handler reaches the request logger.

### Start evlog

| Pattern | Frameworks |
|---------|------------|
| `evlog(options)` middleware / plugin | Hono, Express, Fastify, Elysia, SvelteKit, React Router |
| `createEvlog(options)` factory | Next.js |
| `EvlogModule.forRoot()` | NestJS |
| Module default export | Nuxt, Nitro v2/v3 |
| Manual factory | Cloudflare Workers (`createWorkersLogger`), Standalone, AWS Lambda, Astro |

### Reach the logger from a handler

| Pattern | Frameworks |
|---------|------------|
| `useLogger(event)` | Nuxt, Nitro |
| `useLogger()` | Next.js, NestJS, Express, Fastify, Elysia, SvelteKit, React Router, Hono |
| `c.get('log')` | Hono |
| `req.log` | Express |
| `request.log` | Fastify |
| `event.locals.log` | SvelteKit |
| `context.get(loggerContext)` | React Router |
| `createRequestLogger()` / `createLogger()` | Standalone, Workers, manual setups |

::callout{icon="i-lucide-info" color="info"}
On Hono, use `c.get('log')` inside handlers and `useLogger()` from `evlog/hono` in the layers underneath. See [Hono integration](/integrate/frameworks/hono).
::

## Full-stack frameworks

::card-group
  :::card
  ---
  icon: i-simple-icons-nuxtdotjs
  title: Nuxt
  to: /integrate/frameworks/nuxt
  color: neutral
  ---
  Register `evlog/nuxt` in your modules to get auto-imported `useLogger`, `createError`, and `parseError`.
  :::
  :::card
  ---
  icon: i-simple-icons-nextdotjs
  title: Next.js
  to: /integrate/frameworks/nextjs
  color: neutral
  ---
  `createEvlog()` factory with `withEvlog()` handler wrapper and client provider.
  :::
  :::card
  ---
  icon: i-simple-icons-svelte
  title: SvelteKit
  to: /integrate/frameworks/sveltekit
  color: neutral
  ---
  Handle and handleError hooks with request-scoped logger on `event.locals.log`.
  :::
  :::card
  ---
  icon: i-custom-nitro
  title: Nitro
  to: /integrate/frameworks/nitro
  color: neutral
  ---
  Module for both Nitro v2 and v3 with plugin-based drain and enrichment hooks.
  :::
  :::card
  ---
  icon: i-custom-tanstack
  title: TanStack Start
  to: /integrate/frameworks/tanstack-start
  color: neutral
  ---
  Uses Nitro v3 module with async context, so `useLogger()` works inside server functions. Also covers TanStack Router (full-stack mode).
  :::
  :::card
  ---
  icon: i-custom-reactrouter
  title: React Router
  to: /integrate/frameworks/react-router
  color: neutral
  ---
  Middleware with `context.get(loggerContext)` and `useLogger()` for loaders and services.
  :::
  :::card
  ---
  icon: i-simple-icons-nestjs
  title: NestJS
  to: /integrate/frameworks/nestjs
  color: neutral
  ---
  `EvlogModule.forRoot()` with global middleware, exception filter, and async config.
  :::
::

## Server frameworks

::card-group
  :::card
  ---
  icon: i-simple-icons-express
  title: Express
  to: /integrate/frameworks/express
  color: neutral
  ---
  Middleware with `req.log` and 4-argument error handler.
  :::
  :::card
  ---
  icon: i-simple-icons-hono
  title: Hono
  to: /integrate/frameworks/hono
  color: neutral
  ---
  Middleware with typed `c.get('log')` via `EvlogVariables`.
  :::
  :::card
  ---
  icon: i-simple-icons-fastify
  title: Fastify
  to: /integrate/frameworks/fastify
  color: neutral
  ---
  Plugin with `request.log` that shadows Fastify's built-in pino logger.
  :::
  :::card
  ---
  icon: i-custom-elysia
  title: Elysia
  to: /integrate/frameworks/elysia
  color: neutral
  ---
  Plugin with `log` in route context via Elysia's `derive`.
  :::
  :::card
  ---
  icon: i-lucide-network
  title: oRPC
  to: /integrate/frameworks/orpc
  color: neutral
  ---
  Handler wrapper + procedure middleware exposing `context.log` and per-procedure `operation`.
  :::
  :::card
  ---
  icon: i-simple-icons-cloudflare
  title: Cloudflare Workers
  to: /integrate/frameworks/cloudflare-workers
  color: neutral
  ---
  Factory for creating request-scoped loggers with Cloudflare-specific context.
  :::
  :::card
  ---
  icon: i-custom-lambda
  title: AWS Lambda
  to: /integrate/frameworks/aws-lambda
  color: neutral
  ---
  `initLogger` once per runtime; `createLogger` per invocation (SQS, events, HTTP API).
  :::
  :::card
  ---
  icon: i-simple-icons-typescript
  title: Standalone
  to: /integrate/frameworks/standalone
  color: neutral
  ---
  For scripts, CLI tools, queues, and any TypeScript process.
  :::
  :::card
  ---
  icon: i-lucide-puzzle
  title: Custom Integration
  to: /extend/custom-framework
  color: neutral
  ---
  Build your own middleware with the evlog toolkit API.
  :::
::

::callout{icon="i-lucide-info" color="info"}
All frameworks support the same features: [wide events](/learn/wide-events), [structured errors](/learn/structured-errors), [drain adapters](/integrate/adapters/overview), [enrichers](/use-cases/enrichers), [sampling](/learn/sampling), and [AI SDK integration](/use-cases/ai-sdk/overview).
::

## Strip client logs at build time

For any Vite-based project, the [`evlog/vite` plugin](/reference/vite-plugin) adds build-time optimizations:

- **Auto-initialization**: no `initLogger()` call needed
- **Debug stripping**: `log.debug()` removed from production builds
- **Source location**: inject `__source: 'file:line'` into log calls

It works with SvelteKit, Hono through vite-node, and any Vite-powered setup. Under Nuxt the same three options live on the `evlog/nuxt` module, so the plugin does not need to be added by hand.
